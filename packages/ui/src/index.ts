// Componentes compartidos sitio + vista previa del panel.
// Se poblará en Fase S al convertir cada plantilla (header, footer, hero, cards, ficha, etc.).
export type { Section, PageContent, SiteConfig, ThemeTokens } from '@ofitodo/schema';
